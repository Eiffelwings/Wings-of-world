# Contributing To Wings Of World Backend

Thanks for improving Wings Of World.

## Guidelines

- Keep changes scoped and covered by tests when they touch shared backend behavior.
- Use `Wings Of World` for new user-facing copy and docs.
- Preserve legacy aliases and protocol identifiers unless the migration is deliberate and tested.
- Do not commit secrets, local `.env` files, generated builds, dependency folders, logs, or runtime data.

import { buildChannelConfigSchema } from "mechanical-wings/plugin-sdk/channel-config-schema";
import { z } from "mechanical-wings/plugin-sdk/zod";

export const SynologyChatChannelConfigSchema = buildChannelConfigSchema(
  z
    .object({
      dangerouslyAllowNameMatching: z.boolean().optional(),
      dangerouslyAllowInheritedWebhookPath: z.boolean().optional(),
    })
    .passthrough(),
);
